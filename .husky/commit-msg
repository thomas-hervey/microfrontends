# .husky/pre-push

#!/bin/sh

echo "***********************************"
echo "HUSKY: Running commit-msg checks..."
echo "***********************************"

. "$(dirname -- "$0")/_/husky.sh"

npx --no-install commitlint --edit

echo "***********************************"
echo "HUSKY: Commit-msg checks completed!"
echo "***********************************"
